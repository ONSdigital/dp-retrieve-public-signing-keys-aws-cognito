---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
  - name: dp-retrieve-public-signing-keys-aws-cognito

run:
  path: dp-retrieve-public-signing-keys-aws-cognito/ci/scripts/lint.sh